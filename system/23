# login to the system as a root user
ssh root@server_ip_address

# to change to a new user
su - username

# to check
sudo whoami

#or and type the pass
ssh <domainuser>@<hostname>

#in graphic session:
#login with new user and type the password
